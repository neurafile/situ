{
  "name": "@gadget/vm",
  "version": "0.1.0",
  "description": "JavaScript bindings for Gadget VM (Rust/WASM)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "wasm"
  ],
  "scripts": {
    "build": "npm run build:wasm && npm run build:js",
    "build:wasm": "cd ../rust-vm && make wasm",
    "build:js": "tsc && node scripts/bundle.js",
    "test": "jest",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "gadget",
    "vm",
    "wasm",
    "parser",
    "dsl"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "peerDependencies": {
    "@babylonjs/core": "^6.0.0",
    "three": "^0.150.0"
  },
  "peerDependenciesMeta": {
    "@babylonjs/core": {
      "optional": true
    },
    "three": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0",
    "@babylonjs/core": "^6.0.0",
    "three": "^0.150.0",
    "@types/three": "^0.150.0"
  }
}
